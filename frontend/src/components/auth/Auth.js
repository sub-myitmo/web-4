import React from 'react';
import AuthForm from "./AuthForm";
import './auth.css'

function Auth() {

    return (
        <main className="auth">
            <AuthForm/>
        </main>
    );
}

export default Auth;